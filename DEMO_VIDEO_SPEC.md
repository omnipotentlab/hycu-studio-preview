# HYCU AI Studio — 데모 영상 기능 명세

Claude Design 기반 데모 애니메이션 제작용. 8단계 프로세스 전체를 순서대로 훑되,
각 단계에서 노출 가능한 기능·옵션을 전부 화면에 담는 것을 목표로 한다.
데이터는 AI 리터러시(AIG101) 교과목, 5주차 02교시(생성형 AI의 이해와 업무 활용) 기준.

## 프로세스 전체 흐름 (사이드바 ProcessNav 8단계)

| # | 화면 id | 라벨 | 서브 라벨 |
|---|---|---|---|
| 1 | course-setup | 교과목 정보 | 학사 연동 · 기획서 · CQI |
| 2 | wizard | 교안 설정 | 학습자료 · 구성 · 톤 |
| 3 | outline | 아웃라인 생성 | 실시간 생성 · 검토 · 편집 |
| 4 | rendering | 슬라이드 렌더 | 디자인 적용 |
| 5 | editor (alias: addie, review) | 편집 | 슬라이드 수정 |
| 6 | inspection | 검수 | 전체 규칙 검사 |
| 7 | preview (alias: chroma) | 미리보기 | 최종 확인 |
| 8 | export | 내보내기 | PPTX · PDF · SCORM · MP4 |

이동은 하단 ProcessFooter의 "이전/다음"으로만 순차 진행(상단 ProcessNav는 클릭 불가, 진행 지표 전용) —
데모에서도 이 순차 이동 UX 자체를 한 번은 보여줄 것.

공통으로 항상 떠 있는 셸 요소(각 화면 촬영 시 함께 잡을 것):
- 좌측 Sidebar: 워크스페이스(대시보드/교안 생성/교안 라이브러리), 접기/펼치기
- 상단 Topbar: 브레드크럼, 학사시스템 SSO 연동 배지, 검색, 알림·설정 아이콘
- 상단 ProcessNav: 8단계 진행 인디케이터(완료/진행중/예정 3색상)
- 하단 ProcessFooter: 이전/다음, "N/8 · 현재단계" 카운터, 마지막 단계는 "교안 생성 완료"

---

## 1. 교과목 정보 (course-setup)
학사시스템 SSO 연동으로 교과목 데이터를 가져오는 진입 화면. 로그인 직후 대시보드를 거치지 않고 바로 이 단계로 옴.
- 학사 연동 상태 표시(SSO 세션 배지)
- 교과목 기획서(과목명 · 코드 · 담당교수 · 총 13주 × 3교시 = 39교시) 노출
- CQI(강의평가 개선) 관련 정보 연동 지점

## 2. 교안 설정 (wizard)
- 학습자료 업로드(reference corpus 구성 지점)
- 교안 구성 옵션(주차/교시 선택 — COURSE_PLAN 13주 × 3세션 목록)
- 톤 설정(강의체 격식 등)
- "다음 · 아웃라인 생성"으로만 진행, 자동 실행 없음(수동 시작 게이트)

## 3. 아웃라인 생성 (outline) — 기능 다수, 데모 핵심 구간
- **시작 게이트**: "아웃라인 생성 시작" 버튼 클릭 전엔 대기 화면만 표시
- **실시간 스트리밍 생성**: 슬라이드가 카드로 하나씩 순차 생성(진행률 바 + N/28 카운터), 생성 중 스켈레톤 카드 노출
- **ADDIE 단계 분포 요약**: 분석(A)·설계(D)·개발(Dv)·실행(I)·평가(E) 색상 분포 바 + 슬라이드별 배지
- **포맷 자동 채택**: 콘텐츠 성격에 맞춰 표/순서도/차트/다이어그램/도식/칸반보드/퀴즈카드/이미지/텍스트/없음 10종 중 자동 선택 + 이유 표시
- **카드별 포맷 변경**: "포맷 변경" 버튼 → 콘텐츠 적합 후보 칩만 노출(전체 포맷 대신 축약 후보)
- **카드별 텍스트 수정**: "텍스트 수정" 토글 → 제목 input + 본문 textarea 인라인 편집
- **포맷 미리보기 프리뷰**: 각 카드 하단에 실제 포맷 미니 시각화(표 그리드/칸반 3열/순서도 화살표/차트 막대/퀴즈 4지선다 등)
- **AI 수정 사이드패널**: 슬라이드 선택 → 채팅형 AI 수정 요청("핵심 3줄로 정리" / "비교표로 변경" / "설명을 더 쉽게" 퀵 버튼 + 자유 프롬프트 입력) → 실시간 반영 + 대화 로그

## 4. 슬라이드 렌더 (rendering)
- 아웃라인 확정 → 실제 슬라이드 디자인(마스터 템플릿 슬롯 채우기)으로 렌더링하는 진행 화면
- 퍼센트 진행률 + 렌더 로그 스트리밍 + 슬라이드 실시간 미리보기(처리 청크 단위)
- (원본 v7 커밋 기준 기능 — 화면 상세는 컴파일된 산출물 안에만 존재, 소스 미보유. 데모 촬영 시 실제 앱에서 직접 확인 필요)

## 5. 편집 (editor / 별칭 addie, review)
- 그리드 스타일 슬라이드 목록(gridStyle="grid")
- 번역 스플릿 뷰(translateUI="split", defaultLang="en") — 원문/번역 좌우 대조 편집
- 자막 상시 표시 옵션(captionsAlways)
- ADDIE 단계 매핑 화면(addie 별칭) — 슬라이드별 ADDIE 단계 재배정
- **캔바식 편집**: 콘텐츠 블록·텍스트 요소를 화면에서 직접 선택·드래그·더블클릭 수정(OBJ_SPECS 편집 레이어, 전 슬라이드 공통)
- 내부 상세는 컴파일 산출물에만 존재 — 소스 미보유분은 실제 앱 캡처로 보완 필요

## 6. 검수 (inspection)
- "검수 실행" 버튼으로 수동 시작(자동 실행 없음)
- 전체 규칙 검사(마스터 좌표 · 교수자 안전영역 · 시안 액센트 규칙 등, export 화면의 "품질 검증 통과 28/28" 배지와 연동)
- 상세 UI는 컴파일 산출물에만 존재 — 실제 앱 캡처로 보완 필요

## 7. 미리보기 (preview / 별칭 chroma)
### Preview (학습자 시점)
- 학습자 시점 미리보기 배지
- 큰 슬라이드 프리뷰(ScaledSlide) + ◀◀ ◀ 진행바 ▶ 내비게이션, 좌우 방향키 지원
- 전체 슬라이드 썸네일 그리드(7열) — 클릭으로 바로 이동, 슬라이드 번호 오버레이

### Chroma (교수자 크로마키 합성 시뮬레이터)
- 교수자 영상 합성 안전영역(우측 하단 480×540px) 시각화 + "침범 0건 · 안전" 상태 배지
- **안전 영역 검증 패널**: 교수자 영역 좌표, 본문 침범 px, 로고 위치, 진도바 충돌, ADDIE 인디케이터 — 5개 체크리스트
- **스튜디오 환경 패널**: 카메라 입력(Sony FX3 1920×1080 30fps), 크로마키 색(#00B140), 키 적용(OBS Despill 스무딩), 교수자 위치/크기 조절값
- **좌표 시스템 가이드**: 절대좌표 기준 안내 카드

## 8. 내보내기 (export) — 기능 다수, 데모 핵심 구간
### Export 탭
- **포맷 선택(라디오그룹 4종)**: PowerPoint(.pptx, 편집 가능) / PDF(.pdf, 인쇄용) / SCORM(LMS 패키지 .zip) / 동영상(.mp4, 강의 송출)
- **포함 옵션 체크리스트(7종)**: HYCU 폰트 4종 임베드 · 교수자 내레이션 스크립트(notes) · ADDIE 단계 메타데이터 · 슬라이드 마스터(재사용) · 교수자 크로마키 합성 · 형성평가 정답 표시 · 원본 교안 언어 유지
- **배포 대상(4종)**: HYCU LMS 자동 등록 · 학과 공유 드라이브 · 로컬 다운로드 · 이메일 전송(TA·공동교수)
- **품질 검증 배지**: "28/28 슬라이드 규칙 준수" 통과 표시
- **내보내기 진행**: 시작 → 진행률 바(%) → 완료 시 "다운로드 완료 · 다시 받기"

### 다국어 교안(TranslateHub, export 탭 내)
- 진행 카운터(예: 2/4 언어 승인·검수 진행 중)
- **언어별 카드(그리드)**: 상태별(미번역/번역중/검수대기/승인완료) 다른 색상 상태바 + 진행률
  - 미번역: "일괄 번역 시작" 버튼
  - 번역중: 진행률 표시(비활성 버튼)
  - 검수대기: "검수"/"일괄 승인" 버튼 + 승인·대기 슬라이드 수 + 검수자 표시
  - 승인완료: "보기"/"재번역" 버튼 + 승인자·갱신일 표시
- **전체 일괄 번역 실행**: 모든 미번역 언어 한 번에, 용어집 자동 적용, 소요시간 안내
- **번역 엔진 설정 패널**: 엔진(Claude 3.5 Sonnet 학술모드) · 톤(격식) · 용어집 버전 · 문체 가이드(APA 7판) · 번역 단위 · 예외 처리(인용·고유명사·코드 보호)

### 용어집(Glossary, export 탭 내)
- 검색 + CSV 가져오기 + 용어 추가
- 카테고리 필터 칩
- 언어별 대조 테이블(한국어 원문 기준 각 언어 번역 병기)
- 🔒 잠금 용어 표시(모든 번역에서 절대 변형되지 않음, 학과 단위 공유 가능) 안내

---

## 필수 시연 절차 (사용자 지정, 2026-08-10)

아래 5개는 "노출"이 아니라 데모 영상 안에서 실제로 조작 흐름까지 보여줘야 하는 필수 절차다:

1. **포맷 변경** — 아웃라인 카드에서 "포맷 변경" 클릭 → 후보 칩 펼침 → 다른 포맷 선택 → 카드가 즉시 새 포맷 시각화로 바뀌는 것까지
2. **텍스트 변경** — 카드 "텍스트 수정" 토글 → 제목/본문 인라인 편집 → "수정 완료"로 반영되는 것까지
3. **슬라이드 편집** — 편집(editor) 단계의 캔바식 인라인 편집: 콘텐츠 블록·텍스트 요소 선택→드래그 또는 더블클릭 수정까지
4. **AI 채팅 활용** — 아웃라인 AI 수정 사이드패널: 퀵버튼(핵심 3줄로 정리 등) 또는 자유 프롬프트 입력 → 적용 중 상태 → 반영 결과 + 대화 로그까지
5. **검수** — 검수(inspection) 단계 "검수 실행" 버튼 클릭 → 규칙 검사 진행 → 통과 결과(28/28) 표시까지

## 촬영/커버리지 체크리스트

- [ ] 8단계 ProcessNav를 한 번은 전체 스크롤/확대해서 보여준다
- [ ] 위 필수 시연 절차 5개(포맷변경·텍스트변경·슬라이드편집·AI채팅·검수) 전부 클릭~결과 흐름까지 담는다 — 단순 화면 노출로 끝내지 않는다
- [ ] 아웃라인 카드 포맷 10종 중 최소 5종 이상 실제 카드로 등장시킨다 (표/순서도/차트/다이어그램/퀴즈카드 권장)
- [ ] 내보내기: 4개 포맷 라디오 전환 + 7개 옵션 체크 상태 + 4개 배포 대상 모두 프레임에 담는다
- [ ] 다국어: 4개 상태(미번역/번역중/검수대기/승인완료) 카드가 최소 한 번씩 등장하도록 mock 상태 조정
- [ ] 크로마키 시뮬레이터: 안전영역 오버레이 + 검증 체크리스트 5항목 노출
- [ ] 전 화면에서 데이터는 AI 리터러시(AIG101) 기준으로 통일 — 시장조사론 잔존 문구 없는지 최종 확인

## 알려진 문서화 공백

Dashboard, Library, CourseSetup, Wizard, Generating(렌더 진행), InspectionScreen, AddieMapping, Editor,
ScaledSlide 컴포넌트의 내부 옵션은 소스(work/source-v10, ui repo)에 포함되어 있지 않고 컴파일된
번들 산출물에만 존재한다 (이번 통합으로 이관 예정인 source-v10은 v10 시점 신규 화면 4개만 포함).
데모 촬영 전 실제 앱 실행 화면으로 위 목록의 정확한 옵션을 재확인·보완할 것.
